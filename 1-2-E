#include <vector>
#include <iostream>
 
using namespace std;
 
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int n, k;
    cin >> n >> k;
    vector <int> a;
    for(int i = 0; i < n; i++)
    {
        long long x;
        cin >> x;
        a.emplace(lower_bound(a.begin(), a.end(), x), x);
        if(i > k - 1) a.pop_back();
    }
    for(int i = 0; i < k; i++) cout << a[i] << " ";
    return 0;
}
